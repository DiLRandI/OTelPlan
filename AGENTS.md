# Agent Instructions

Read `docs/CODEX_IMPLEMENTATION_PROMPT.md` and all specification documents before implementing.

Do not reduce the project to naming-convention matching.

The product must remain:

- architecture agnostic;
- policy driven;
- source-code independent;
- OpenTelemetry native;
- safe by default;
- deterministic and explainable.

Treat `docs/14_ACCEPTANCE_CRITERIA.md` as release-blocking.
