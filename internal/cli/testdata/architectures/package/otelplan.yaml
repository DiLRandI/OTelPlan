apiVersion: otelplan.io/v1alpha1
kind: InstrumentationPlan
backend: {name: otelc, version: v1.1.0}
project: {packages: [./...]}
rules:
- id: checkout
  match:
    methods: [Submit]
  span: {name: checkout}
- id: payment
  match:
    methods: [Authorize]
  span: {name: payment}
